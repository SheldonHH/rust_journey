// pub fn add(left: usize, right: usize) -> usize {
//     left + right
// }
//! My Create
//! 
//! 'my_crate' is a collection of utiltieis to make peform certain caclualtions more efficient
/// 给一个数加一
/// # Example
/// ```
/// let five = 5;
/// 
/// assert_eq!(6, mylib::add_one(5));
/// ```
pub fn add_one(x: i32) -> i32{
    x + 1
}













// #[cfg(test)]
// mod tests {
//     use super::*;

//     #[test]
//     fn it_works() {
//         let result = add(2, 2);
//         assert_eq!(result, 4);
//     }
// }
